pipeline {
    agent any 
    environment {
        GITHUB_CREDS = credentials("github-account")
    }
    stages {
        stage('BUILD') {
            steps {
                echo 'naming server jenkinsfile' 
            }
        }
    }
}